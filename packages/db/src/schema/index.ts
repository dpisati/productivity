/**
 * Drizzle schema barrel. Per-domain table definitions and relations are added
 * here in M2 (users, settings, categories, finance, tasks, reminders,
 * integrations, notifications, audit logs).
 */
export {};
